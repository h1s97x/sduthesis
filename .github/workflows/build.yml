name: Build LaTeX Document

on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      
      - name: Install fonts
        run: |
          apt-get update && apt-get install -y --no-install-recommends \
            fonts-noto-core \
            fonts-texgyre \
            fonts-freefont-ttf \
            fontconfig
          fc-cache -fv

      - name: Prepare CI environment
        run: |
          # 在 CI 环境中使用开源字体，临时覆盖用户配置
          if [ "$CI" = "true" ]; then
            cat >> config/main/config-main.tex << 'FONTCI'

          % CI 环境字体配置（自动添加）
          \setmainfont{TeX Gyre Termes}
          \setsansfont{TeX Gyre Heros}
          \setmonofont{TeX Gyre Cursor}
          FONTCI
          fi

      # 使用 latexmk 自动处理多次编译
      # latexmk 会自动运行: xelatex → biber → xelatex → xelatex 直到稳定
      - name: Compile LaTeX with latexmk
        uses: xu-cheng/latex-action@v3
        with:
          root_file: main.tex
          compiler: latexmk
          args: -xelatex -synctex=1 -interaction=nonstopmode -halt-on-error -file-line-error
      
      - name: Upload PDF artifact
        uses: actions/upload-artifact@v4
        with:
          name: thesis-pdf
          path: main.pdf
